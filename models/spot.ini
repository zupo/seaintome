[model]
name = Spot
label = {{ this.title }}
hidden = yes

[children]
replaced_with = site.query('/sessions').filter(F.spot == this)

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[fields.pois]
label = POIs
type = strings


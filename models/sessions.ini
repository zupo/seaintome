[model]
name = Sessions
label = Sessions
hidden = yes
protected = yes

[children]
model = session
order_by = -pub_date, title
slug_format = {{ this._id.replace('_', '/') }} - {{ this.spot }}

[pagination]
enabled = yes
per_page = 10
